services:
  max-test-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: max-test-bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${PORT:-8090}:${PORT:-8090}"
